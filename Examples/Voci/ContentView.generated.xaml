<!-- Generated by Loom. Review diagnostics before integrating. -->
<Grid
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Source: /private/SDF/Voci/platform/macos/VociMacApp.swift; ContentView.body -->
    <!-- LOOM101: GeometryReader depends on runtime size proposals; generated XAML preserves structure but requires an explicit sizing rule. -->
    <Grid MinWidth="1180" MinHeight="720">
        <!-- GeometryReader: connect runtime width/height through SizeChanged or the view model. -->
        <Grid>
            <Border Background="{ThemeResource VociCanvasBrush}" />
            <Grid ColumnSpacing="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <!-- SwiftUI condition: model.isThemeEditorPresented. Bind Visibility or use VisualStateManager. -->
                    <Grid>
                        <!-- Loom component reference: themeEditorDrawer. Generate separately or map to a UserControl. -->
                    </Grid>
                </Grid>
                <Grid Grid.Column="1" RowSpacing="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="10,10,10,6">
                        <!-- Loom component reference: operatorTopBar. Generate separately or map to a UserControl. -->
                    </Grid>
                    <Grid Grid.Row="1" Margin="10,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="190*" MinWidth="170" MaxWidth="240" />
                            <ColumnDefinition Width="*" MinWidth="960" />
                        </Grid.ColumnDefinitions>
                        <!-- SplitView: add pointer-driven resizing or a project-specific splitter control. -->
                        <Grid Grid.Column="0" MinWidth="170" MaxWidth="240">
                            <!-- Loom component reference: singersColumn. Generate separately or map to a UserControl. -->
                        </Grid>
                        <Grid Grid.Column="1" MinWidth="960">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="620*" MinWidth="420" />
                                <ColumnDefinition Width="680*" MinWidth="520" />
                            </Grid.ColumnDefinitions>
                            <Grid Grid.Column="0" MinWidth="420">
                                <!-- Loom component reference: selectedQueueColumn. Generate separately or map to a UserControl. -->
                            </Grid>
                            <Grid Grid.Column="1" MinWidth="520">
                                <!-- Loom component reference: playbackColumn. Generate separately or map to a UserControl. -->
                            </Grid>
                        </Grid>
                    </Grid>
                    <Grid Grid.Row="2" Margin="10,8">
                        <!-- Loom component reference: bottomStatusBar. Generate separately or map to a UserControl. -->
                    </Grid>
                </Grid>
            </Grid>
            <Grid>
                <!-- SwiftUI condition: model.searchOverlayOpen. Bind Visibility or use VisualStateManager. -->
                <Grid>
                    <!-- Loom component reference: searchOverlay. Generate separately or map to a UserControl. -->
                </Grid>
            </Grid>
            <Grid>
                <!-- SwiftUI condition: model.isSettingsPresented. Bind Visibility or use VisualStateManager. -->
                <Border Background="{ThemeResource VociCanvasBrush}" />
                <Grid>
                    <!-- Loom component reference: SettingsDialog. Generate separately or map to a UserControl. -->
                </Grid>
            </Grid>
            <Grid>
                <!-- SwiftUI condition: model.isLibraryPresented. Bind Visibility or use VisualStateManager. -->
                <Border Background="{ThemeResource VociCanvasBrush}" />
                <Grid>
                    <!-- Loom component reference: LibraryDialog. Generate separately or map to a UserControl. -->
                </Grid>
            </Grid>
            <Grid>
                <!-- SwiftUI condition: isKaraokeLyricsDialogPresented. Bind Visibility or use VisualStateManager. -->
                <Border Background="{ThemeResource VociCanvasBrush}" />
                <Grid>
                    <!-- Loom component reference: karaokeLyricsDialog. Generate separately or map to a UserControl. -->
                </Grid>
            </Grid>
            <Grid>
                <!-- SwiftUI condition: model.isFillerTunesPresented. Bind Visibility or use VisualStateManager. -->
                <Border Background="{ThemeResource VociCanvasBrush}" />
                <Grid>
                    <!-- Loom component reference: FillerTunesDialog. Generate separately or map to a UserControl. -->
                </Grid>
            </Grid>
            <Grid>
                <!-- SwiftUI condition: model.isHistoryPresented. Bind Visibility or use VisualStateManager. -->
                <Border Background="{ThemeResource VociCanvasBrush}" />
                <Grid>
                    <!-- Loom component reference: HistoryDialog. Generate separately or map to a UserControl. -->
                </Grid>
            </Grid>
            <Grid>
                <!-- SwiftUI condition: model.isStatisticsPresented. Bind Visibility or use VisualStateManager. -->
                <Border Background="{ThemeResource VociCanvasBrush}" />
                <Grid>
                    <!-- Loom component reference: StatisticsDialog. Generate separately or map to a UserControl. -->
                </Grid>
            </Grid>
            <Grid>
                <!-- SwiftUI condition: model.isDJProfilesPresented. Bind Visibility or use VisualStateManager. -->
                <Border Background="{ThemeResource VociCanvasBrush}" />
                <Grid>
                    <!-- Loom component reference: DJProfilesDialog. Generate separately or map to a UserControl. -->
                </Grid>
            </Grid>
            <Grid>
                <!-- SwiftUI condition: model.isShortcutsPresented. Bind Visibility or use VisualStateManager. -->
                <Border Background="{ThemeResource VociCanvasBrush}" />
                <Grid>
                    <!-- Loom component reference: ShortcutsDialog. Generate separately or map to a UserControl. -->
                </Grid>
            </Grid>
            <Grid>
                <!-- SwiftUI condition: !model.quitProtectionOverlayMessage.isEmpty. Bind Visibility or use VisualStateManager. -->
                <Grid>
                    <!-- Loom component reference: quitProtectionOverlay. Generate separately or map to a UserControl. -->
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Grid>

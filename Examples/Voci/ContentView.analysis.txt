Loom analysis
Source: /private/SDF/Voci/platform/macos/VociMacApp.swift
View: ContentView.body
Swift syntax nodes: 57975
Layout nodes: 43
Component references: 16

Layout tree
└─ geometryReader: GeometryReader
   └─ overlayStack: ZStack
      ├─ color: VociUITheme.windowBackground
      ├─ horizontalStack: HStack(spacing: 0)
      │  ├─ conditional: if
      │  │  └─ component: themeEditorDrawer
      │  └─ verticalStack: VStack(spacing: 0)
      │     ├─ component: operatorTopBar
      │     ├─ splitView: HSplitView
      │     │  ├─ component: singersColumn
      │     │  └─ horizontalStack: HStack(spacing: VociUILayout.grid)
      │     │     ├─ component: selectedQueueColumn
      │     │     └─ component: playbackColumn
      │     └─ component: bottomStatusBar
      ├─ conditional: if
      │  └─ component: searchOverlay
      ├─ conditional: if
      │  ├─ color: Color.black.opacity(0.38)
      │  └─ component: SettingsDialog
      ├─ conditional: if
      │  ├─ color: Color.black.opacity(0.38)
      │  └─ component: LibraryDialog(onClose: { model.isLibraryPresented = false })
      ├─ conditional: if
      │  ├─ color: Color.black.opacity(0.38)
      │  └─ component: karaokeLyricsDialog
      ├─ conditional: if
      │  ├─ color: Color.black.opacity(0.38)
      │  └─ component: FillerTunesDialog(onClose: { model.isFillerTunesPresented = false })
      ├─ conditional: if
      │  ├─ color: Color.black.opacity(0.38)
      │  └─ component: HistoryDialog(onClose: { model.isHistoryPresented = false })
      ├─ conditional: if
      │  ├─ color: Color.black.opacity(0.38)
      │  └─ component: StatisticsDialog(onClose: { model.isStatisticsPresented = false })
      ├─ conditional: if
      │  ├─ color: Color.black.opacity(0.38)
      │  └─ component: DJProfilesDialog(onClose: { model.isDJProfilesPresented = false })
      ├─ conditional: if
      │  ├─ color: Color.black.opacity(0.38)
      │  └─ component: ShortcutsDialog(onClose: { model.isShortcutsPresented = false model.capturingHotkeyAction = n…)
      └─ conditional: if
         └─ component: quitProtectionOverlay

Diagnostics: 8
  [info] LOOM004 Modifier .onAppear has a closure that belongs in WinUI behavior or a template mapping, not the generated visual tree.
  [info] LOOM004 Modifier .onChange has a closure that belongs in WinUI behavior or a template mapping, not the generated visual tree.
  [info] LOOM004 Modifier .onChange has a closure that belongs in WinUI behavior or a template mapping, not the generated visual tree.
  [info] LOOM004 Modifier .onChange has a closure that belongs in WinUI behavior or a template mapping, not the generated visual tree.
  [info] LOOM004 Modifier .onChange has a closure that belongs in WinUI behavior or a template mapping, not the generated visual tree.
  [info] LOOM004 Modifier .onChange has a closure that belongs in WinUI behavior or a template mapping, not the generated visual tree.
  [info] LOOM004 Modifier .onChange has a closure that belongs in WinUI behavior or a template mapping, not the generated visual tree.
  [warning] LOOM101 GeometryReader depends on runtime size proposals; generated XAML preserves structure but requires an explicit sizing rule.
